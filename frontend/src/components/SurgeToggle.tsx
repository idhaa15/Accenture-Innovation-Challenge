import { Check, Zap } from 'lucide-react';
export default function SurgeToggle({onClick,disabled,active=false}:{onClick:()=>void;disabled:boolean;active?:boolean}){return <button className="surge" title="Adds up to 10 simulated catalogue patients, triages them, and re-ranks the queue." disabled={disabled} onClick={onClick}>{active?<Check size={15}/>:<Zap size={15}/>} {active?'3x surge active · Reset':'Simulate 3x surge · add 10 patients'}</button>}
